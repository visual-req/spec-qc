# 仕組み（動作原理）

![仕組み（動作原理）](../assets/work-principle.svg)

要点：

- 需要仕様（Word/Markdown/フォルダ）は最初に解析・クレンジング・分割（チャンク化）される
- 各チャンクをルール/プロンプトと結合して LLM を呼び出し、構造化 JSON の出力を要求する
- 返却結果を解析・検証し、レポート（例：Excel）へ集約する。トラブルシュートのため主要ログも記録する
